# 直接依賴（程式直接 import 的）。完整含 transitive 的鎖檔見 requirements.lock，
# render.yaml 實際安裝走 lock 以求可重現。升級流程：改這裡 → 乾淨環境 pip install
# → pip freeze 重生 requirements.lock → 部署觀察。
fastapi==0.136.3
uvicorn==0.49.0
line-bot-sdk==3.23.0
gspread==6.2.1
google-auth==2.53.0
anthropic==0.107.1
pytz==2026.2
httpx==0.28.1
websockets==16.0
